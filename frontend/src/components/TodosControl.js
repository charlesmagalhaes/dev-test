import React from 'react'

export default function TodosControl(todos) {
  return (
    <div className="container card-panel teal lighten-8">
          {/* {
          todos.map(({id, description, date}) =>{
            return(
                <div key={id}>
              
                </div>
            );
          })
          }; */}
          
      </div>

  );
}
